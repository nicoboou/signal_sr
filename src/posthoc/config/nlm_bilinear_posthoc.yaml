seed: 42
device: cuda:2
output_dir: posthoc/outputs/nlm_bilinear
run_id: null

sr:
  config: sr/configs/train/nlm_pixel_hr_linear_uncond.yaml
  checkpoint: sr/outputs/nlm_pixel_uncond_hr_20260618_1831
  method: dps
  split: test
  batch_size: 16

classifier:
  checkpoint: /projects/compures/nicolas/signal_sr/src/prehoc/outputs/nlm_bilinear_sweep/20260622_1804/classifiers/bilinear_level1p0_seed0_fold1.pt
  threshold: 0.5

data:
  mask_outputs_root: /projects/compures/nicolas/cell_annotator/outputs
  num_workers: 0

degradation:
  type: bilinear
  levels: [1, 2, 4, 6, 8, 16, 32, 64]
  batch_size: 128
  downsample_mode: area
  upsample_mode: nearest

detector:
  config: {}

eval:
  max_samples: null
  save_images: true
